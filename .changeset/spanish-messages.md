---
"@magic-style/vue": minor
---

Spanish dictionary included (`msMessagesEs`): `<MsProvider locale="es-ES">` or `createMsI18n({ locale: "es-MX" })` translates the components' built-in strings into Spanish, with no extra setup.
