# Magic-Style — magic-style

Monorepo fonte do Magic-Style: packages core (`@magic-style/vue`, `css`, `tailwind`, `cli`, `schema`), subsistemas standalone (`datatable`, `calendar`, `charts`, `notifications`) e apps internas (Storybook, visual tests, Reference Application).

Baseline normativo: **Architecture Baseline 1.2** (`../temp/Magic-Style-Architecture-Baseline-1.2/`).

Status: M0 — bootstrap concluído, health gate `pnpm validate` verde.
